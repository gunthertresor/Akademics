<div class= "teacher-container">
  <div class = "container">
    <div clas ="row">
        <% @courses.each do |course| %>
        <div class = "col-4">
          <%= link_to teacher_course_path(course), class: 'course-box m-2' do %>
                <%# <div class="course-box"> %>
                    <h3><%= course.title %></h3>
                    <h2><%= course.name %></h2>

                    <%# <% @courses.classes.each do |classe| %> %>
                        <%# <%= classe.id %> %>
                    <%# <% end %> %>
            <% end %>
          </div>
        <% end %>

    </div>

    <%= link_to "Create a new course", "courses/new", class: "btn-aka" %>
    <%# </div> %>

  </div>
</div>
